"""
agent/
------
LangGraph ReAct agent package for Analyst AI.

Modules:
    graph   — builds and runs the LangGraph agent
    tools   — all LangChain tools exposed to the agent
    prompts — system prompt builder
"""